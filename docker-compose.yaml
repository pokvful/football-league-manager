version: "3"

services:
  db:
    build: .
    environment:
      ACCEPT_EULA: Y
      SA_PASSWORD: Football!
      MSSQL_PID: enterprise
    volumes:
      - ./dockerFiles:/data/sql-files
    ports:
      - ${DATABASE_PORT:-1433}:1433
